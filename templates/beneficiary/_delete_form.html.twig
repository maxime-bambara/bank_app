<form method="post" action="{{ path('app_banker_beneficiary_delete', {'id': beneficiary.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ beneficiary.id) }}">
    <button class="btn">Supprimer</button>
</form>

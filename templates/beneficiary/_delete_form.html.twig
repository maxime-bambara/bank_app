<form method="post" action="{{ path('app_banker_beneficiary_delete', {'id': beneficiary.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce bénéficiaire?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ beneficiary.id) }}">
    <button class="btn btn-warning">Supprimer</button>
</form>

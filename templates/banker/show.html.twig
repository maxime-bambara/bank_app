{% extends 'base.html.twig' %}

{% block title %}Banker{% endblock %}

{% block body %}
    <h1>Banker</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ banker.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ banker.username }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ banker.roles ? banker.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ banker.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_banker_index') }}">back to list</a>

    <a href="{{ path('banker_edit', {'id': banker.id}) }}">edit</a>

    {{ include('banker/_delete_form.html.twig') }}
{% endblock %}
